# RuoYi-AI 一键启动全部服务
# 使用方式: docker-compose up -d
#
# 包含服务:
#   - MySQL 8.0 (数据库，包含初始化SQL)
#   - Redis 6.2 (缓存)
#   - Weaviate (向量数据库)
#   - MinIO (对象存储)
#   - RuoYi-Backend (后端服务)
#   - RuoYi-Admin (管理端前端)
#   - RuoYi-Web (用户端前端)
#
# 镜像仓库地址: crpi-31mraxd99y2gqdgr.cn-beijing.personal.cr.aliyuncs.com/ruoyi_ai

services:
  # ==================== MySQL 数据库 ====================
  mysql:
    # 阿里云镜像地址（包含初始化SQL）
    image: crpi-31mraxd99y2gqdgr.cn-beijing.personal.cr.aliyuncs.com/ruoyi_ai/mysql:v3
    container_name: ruoyi-ai-mysql
    restart: always
    ports:
      - "23306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: ruoyi-ai-agent
      TZ: Asia/Shanghai
    volumes:
      - mysql-data:/var/lib/mysql
    healthcheck:
      # Force TCP: during first-time initialization MySQL exposes a temporary
      # socket-only server. A socket ping would mark the service healthy before
      # the real network listener (used by the backend) is ready.
      test: ["CMD", "mysqladmin", "ping", "--protocol=tcp", "-h", "127.0.0.1", "-u", "root", "-proot"]
      interval: 15s
      timeout: 10s
      retries: 10
      # The bundled initialization SQL can take more than four minutes on a
      # fresh volume. Do not fail dependent services while it is still loading.
      start_period: 300s
    networks:
      - ruoyi-net

  # ==================== Redis 缓存 ====================
  redis:
    image: crpi-31mraxd99y2gqdgr.cn-beijing.personal.cr.aliyuncs.com/ruoyi_ai/redis:6.2
    container_name: ruoyi-ai-redis
    restart: always
    ports:
      - "26379:6379"
    volumes:
      - redis-data:/data
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - ruoyi-net

  # ==================== Weaviate 向量数据库 ====================
  weaviate:
    image: crpi-31mraxd99y2gqdgr.cn-beijing.personal.cr.aliyuncs.com/ruoyi_ai/weaviate:1.30.0
    container_name: ruoyi-ai-weaviate
    restart: always
    ports:
      - "28080:8080"
    environment:
      QUERY_DEFAULTS_LIMIT: 25
      AUTHENTICATION_ANONYMOUS_ACCESS_ENABLED: "true"
      PERSISTENCE_DATA_PATH: /var/lib/weaviate
      DEFAULT_VECTORIZER_MODULE: none
      ENABLE_MODULES: text2vec-cohere,text2vec-huggingface,text2vec-palm,text2vec-openai,generative-openai,generative-cohere,generative-palm,ref2vec-centroid,reranker-cohere,qna-openai
      CLUSTER_HOSTNAME: node1
    volumes:
      - weaviate-data:/var/lib/weaviate
    networks:
      - ruoyi-net

  # ==================== MinIO 对象存储 ====================
  minio:
    image: crpi-31mraxd99y2gqdgr.cn-beijing.personal.cr.aliyuncs.com/ruoyi_ai/minio:latest
    container_name: ruoyi-ai-minio
    restart: always
    ports:
      - "29000:9000"
      - "29090:9090"
    environment:
      MINIO_ROOT_USER: ruoyi
      MINIO_ROOT_PASSWORD: ruoyi123
    volumes:
      - minio-data:/data
    command: server /data --console-address ":9090"
    networks:
      - ruoyi-net

  # ==================== RuoYi-AI 后端服务 ====================
  backend:
    image: ruoyi-ai-backend:latest
    build:
      context: ../../..
      dockerfile: docs/docker/ruoyi-ai/Dockerfile.backend
    container_name: ruoyi-ai-backend
    restart: always
    ports:
      - "26039:6039"
    environment:
      TZ: Asia/Shanghai
      # MySQL 配置
      SPRING_DATASOURCE_DYNAMIC_PRIMARY: master
      SPRING_DATASOURCE_DYNAMIC_DATASOURCE_MASTER_DRIVERCLASSNAME: com.mysql.cj.jdbc.Driver
      SPRING_DATASOURCE_DYNAMIC_DATASOURCE_MASTER_URL: jdbc:mysql://mysql:3306/ruoyi-ai-agent?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=GMT%2B8&autoReconnect=true&rewriteBatchedStatements=true
      SPRING_DATASOURCE_DYNAMIC_DATASOURCE_MASTER_USERNAME: root
      SPRING_DATASOURCE_DYNAMIC_DATASOURCE_MASTER_PASSWORD: root
      # 故障诊断遥测数据库（real_data / real_data_01~03 所在库），只读查询
      SPRING_DATASOURCE_DYNAMIC_DATASOURCE_DCMA_DRIVERCLASSNAME: com.mysql.cj.jdbc.Driver
      SPRING_DATASOURCE_DYNAMIC_DATASOURCE_DCMA_URL: jdbc:mysql://10.108.12.164:3306/dcma?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=GMT%2B8&autoReconnect=true&rewriteBatchedStatements=true&allowPublicKeyRetrieval=true&nullCatalogMeansCurrent=true
      SPRING_DATASOURCE_DYNAMIC_DATASOURCE_DCMA_USERNAME: root
      SPRING_DATASOURCE_DYNAMIC_DATASOURCE_DCMA_PASSWORD: "707707"
      # Redis 配置
      SPRING_DATA_REDIS_HOST: redis
      SPRING_DATA_REDIS_PORT: 6379
      SPRING_DATA_REDIS_DATABASE: 0
      # 日志配置
      LOGGING_LEVEL_ORG_RUOYI: info
      LOGGING_LEVEL_ORG_SPRINGFRAMEWORK: warn
      # 内部故障工具认证；从宿主机环境或 --env-file 注入，禁止提交真实 Token。
      FAULT_INTERNAL_TOOLS_TOKEN: ${FAULT_INTERNAL_TOOLS_TOKEN:-}
      SYS_UPLOAD_PATH: /ruoyi/upload
      # 知识库向量存储配置（必须配置在后端容器）
      VECTOR_STORE_TYPE: weaviate
      VECTOR_STORE_WEAVIATE_PROTOCOL: http
      VECTOR_STORE_WEAVIATE_HOST: weaviate:8080
      VECTOR_STORE_WEAVIATE_CLASSNAME: LocalKnowledge
    volumes:
      - logs-data:/ruoyi/server/logs
      - upload-data:/ruoyi/upload
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_started
    networks:
      - ruoyi-net

  # ==================== RuoYi-AI 管理端前端 ====================
  admin-frontend:
    image: crpi-31mraxd99y2gqdgr.cn-beijing.personal.cr.aliyuncs.com/ruoyi_ai/ruoyi-ai-admin:latest
    container_name: ruoyi-ai-admin
    restart: always
    ports:
      - "25666:5666"
    environment:
      # 后端 API 地址 - 运行时动态配置（无需重新构建镜像）
      # nginx upstream 配置不需要 http:// 前缀，直接使用 host:port
      UPSTREAM_HOST: backend:6039
    # 资源限制 - 防止 CPU 和内存耗尽
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 3G
        reservations:
          cpus: '1'
          memory: 1G
    depends_on:
      - backend
    networks:
      - ruoyi-net

  # ==================== RuoYi-AI 用户端前端 ====================
  web-frontend:
    image: ruoyi-ai-web:latest
    build:
      context: ../../../../ruoyi-web
      dockerfile: Dockerfile.frontend
    container_name: ruoyi-ai-web
    restart: always
    ports:
      - "25137:5137"
    environment:
      UPSTREAM_URL: http://backend:6039
    depends_on:
      - backend
    networks:
      - ruoyi-net

# ==================== 网络配置 ====================
networks:
  ruoyi-net:
    driver: bridge

# ==================== 数据卷配置 ====================
volumes:
  mysql-data:
  redis-data:
  weaviate-data:
  minio-data:
  logs-data:
  upload-data:
